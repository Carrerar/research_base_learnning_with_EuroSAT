# Tầng 2 — T2-H3c: ablation NDBI riêng lẻ. CLAUDE.md mục 6.
tier: 2
exp_id: T2-H3c
config_desc: 13bands+NDBI
input_type: 13bands+ndbi

bands: all
indices: [NDBI]

seeds: [42]

model:
  name: resnet50
  pretrained: true

training:
  lr: 1.0e-4
  weight_decay: 1.0e-4
  batch_size: 32
  epochs: 50
  patience: 10
  label_smoothing: 0.1
  max_grad_norm: 1.0
  num_workers: 4

wandb:
  question: "NDBI một mình đóng góp bao nhiêu trên nền 13 bands?"
  hypothesis: "NDBI = (B11-B08)/(B11+B08) tách built-up → cải thiện trên Industrial / Residential / Highway."
  baseline_ref: "T1-R2 (13bands) seed42 — paired."
  expected: "F1 Industrial / Residential / Highway tăng nhẹ; có thể giúp tách Industrial khỏi Highway (vốn dễ nhầm trong city scene)."
